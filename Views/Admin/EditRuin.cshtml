@model RuinsandMuseums.Models.ViewModels.RuinsAndMuseumsViewModel
@{
    ViewData["Title"] = "Edit Ruin";
}

<div class="container mt-4">
    <h1>Edit Ruin</h1>

    <div class="row">
        <div class="col-md-8">
            <form asp-action="EditRuin" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="NewRuin.Id" />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group mb-3">
                    <label asp-for="NewRuin.Name">Name</label>
                    <input asp-for="NewRuin.Name" class="form-control" required />
                    <span asp-validation-for="NewRuin.Name" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="NewRuin.Location">Location</label>
                    <input asp-for="NewRuin.Location" class="form-control" required />
                    <span asp-validation-for="NewRuin.Location" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="NewRuin.Description">Description</label>
                    <textarea asp-for="NewRuin.Description" class="form-control" rows="3" required></textarea>
                    <span asp-validation-for="NewRuin.Description" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="NewRuin.ImageFile">New Image (optional)</label>
                    <input type="file" asp-for="NewRuin.ImageFile" class="form-control" accept="image/*" />
                    @if (!string.IsNullOrEmpty(Model.NewRuin.ImagePath))
                    {
                        <div class="mt-2">
                            <p>Current Image:</p>
                            <img src="@Model.NewRuin.ImagePath" alt="@Model.NewRuin.Name" class="img-thumbnail" style="max-width: 200px;" />
                        </div>
                    }
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <a href="@Url.Action("ManageRuinsAndMuseums")" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 